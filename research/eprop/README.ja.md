# W2：e-prop 認知層

[中文](README.md) · [English](README.en.md)

**⚠️ 破壊的変更の免責期間：2027-09 まで**（プロジェクト計画書 §12.5、ADR-0005）。

## どの命題を検証しているのか

プロジェクト計画書 §3.1 の認知層学習則：**e-prop（ALIF + 適応閾値 + 資格痕跡）が順序
タスクで動き、かつ活動ニューロン比率が 60% を超えるか**——後者は §七 フェーズ 1 の
ハード指標であり、§3.1「死んだニューロンの防止」の発動ラインでもあります。

## 現時点の結論

**達成。テスト精度 77.48%、活動ニューロン比率 1.0000。**

計画書は「動く」の数値を与えていないため、検証可能な二点に分解しました。両方通っています：

| 指標 | 実測 | 判定基準 |
| :--- | ---: | :--- |
| テスト精度（sMNIST） | **0.7748** | 偶然水準を大きく上回る（10 クラス基线 0.10） |
| **活動ニューロン比率** | **1.0000** | §七：「> 60%」 |
| スパイク疎度 | 0.8527 | §九 はフェーズ 2 で閾値（> 90%）を設けます。今回は判定に使いません |

検証精度は第 1 エポックの 0.4165 から第 30 エポックの 0.7695 まで上昇し、まだ上昇中
でした——したがってこれは**収束値ではなく**、30 エポックという予算での結果です。

再現記録（`research/common/provenance.py` が生成）：

```text
実験名：eprop/smnist
日付：2026-09-23 06:59:54 中国標準時
Git commit：75196c9094f01abe843c1530f5037c39f5e04d9a
Git 状態：クリーン
Python：3.12.14
OS / アーキテクチャ：Windows 11 / AMD64
ハードウェア：NVIDIA GeForce RTX 5060，8,123 MB，sm_120
乱数シード：base=0；グローバルシード=1741413617；検証セット分割=1435468909；重み初期化=3486733650；バッチシャッフル=3990329895；スパイク符号化=1681094898
依存スナップショット：uv.lock sha256=cbafb161e71421f9f228e23e2ab6f4f742f899e9958d290f214dac1dd948e37f
実行コマンド：train_sequential.py --device cuda
所要時間：1753.6 s
ピークメモリ：1,104.1 MiB（§6.2 の硬い制約 8GB）
§6.2 の縮退経路：未発動
備考：n_rec=256, beta=0.07, thr=0.62, eta_in=0.002, eta_rec=0.002, eta_out=0.05, decay_out=0.95
備考：入力符号化：画素値を発火確率とするベルヌーイサンプリング（本プロジェクト自身の選択）
```

GPU メモリは 8 GB のうち 1.08 GB。§6.2 の三つの縮退パスはいずれも発動していません。

### SHD の傍証：**実行したが、学習しなかった**（負の結果として記録）

| 指標 | 実測 | 判定基準 |
| :--- | ---: | :--- |
| テスト精度（SHD、20 クラス） | **0.0534** | 偶然水準 0.05——**学習していない** |
| 活動ニューロン比率 | 1.0000 | §七：「> 60%」✓ |
| スパイク疎度 | 0.9385 | §九 はフェーズ 2 で閾値を設けます。今回は判定に使いません |

sMNIST 用に選んだ同じハイパーパラメータ（`n_rec=256`、30 エポック、`eta_in=eta_rec=2e-3`、
`eta_out=5e-2`、`beta=0.07`、`thr=0.62`）は SHD では**学習しません**：訓練交差エントロピーは
2.9948 から 2.9529 へ動くだけで、`ln 20 = 2.996`——ずっと偶然水準のままです。

**診断**（`research/eprop/neurons.py` の `w_in` 初期化と、前向きのプローブ）：

* `w_in` は `1/√n_in` で初期化されるため、入力 700 チャネルでは各重みの平均振幅が 28 チャネルの
  場合の 1/5 になります（実測 **0.0302** 対 sMNIST の **0.1507**）；
* そのため隠れ層の発火率も sMNIST のおよそ半分です（**0.0526** 対 **0.0997**）——死んでは
  いませんが、駆動が弱い；
* SHD にはさらに 20 クラス・100 タイムステップという負担があります（sMNIST は 10 クラス・
  784 ステップ）。

論文の SHD 設定は**別のもの**です：450 ニューロン、100 エポック、α=0.97、β=0.96、v_th=1.0、
Adam 1e-4、batch 128（M&M 3.1 と Table 1）。**本フェーズは SHD 向けの調整を行っていません**。
したがってこの結果が示すのは「このハイパーパラメータでは足りない」であり、**「規則が適用できない」
でも「実装が間違っている」でもありません**——同じコードが sMNIST では 77.48% です。対比可能な
傍証にするには、sMNIST の設定を押し切るのではなく、SHD 自身のハイパーパラメータ予算（規模、
エポック、α/β/v_th）を与える必要があります。

再現記録（`--dataset shd --device cuda`、ローカル GPU、約 24 分）：

```text
实验名称：eprop/shd
日期：2026-09-23 08:32:19 中国标准时间
Git commit：e2d754ffe18f3ace20a735bbd6668bf94298e859
Git 状态：干净
Python：3.12.14
操作系统 / 架构：Windows 11 / AMD64
硬件：NVIDIA GeForce RTX 5060，8,123 MB，sm_120
随机种子：base=0；全局种子=1741413617；划分验证集=1435468909；权重初始化=3486733650；每批打乱=3990329895；脉冲编码=1681094898
依赖快照：uv.lock sha256=cbafb161e71421f9f228e23e2ab6f4f742f899e9958d290f214dac1dd948e37f
运行命令：train_sequential.py --dataset shd --device cuda
耗时：1413.7 s
显存峰值：3,220.6 MiB（§6.2 硬约束 8GB）
§6.2 降级路径：未触发
备注：n_rec=256, beta=0.07, thr=0.62, eta_in=0.002, eta_rec=0.002, eta_out=0.05, decay_out=0.95
备注：输入编码：SHD 脉冲时刻铺到 100 个箱、箱内二值化（本项目自己的选择）
```

## どうやって実行して結果を出すのか

```bash
uv run python scripts/download_data.py mnist              # 初回：MNIST をダウンロードして検証
uv run python -m research.eprop.train_sequential          # 受入実行（ローカル GPU、約 25 分）
uv run python -m research.eprop.train_sequential --smoke  # CPU スモーク、CI 専用
```

傍証データセット SHD は別途約 169 MB のダウンロードが必要です：
`uv run python scripts/download_data.py shd`。**実行しましたが、学習しませんでした**——
下の「SHD の傍証」を参照。

## 方程式とモジュールの対応

| 論文 | 内容 | モジュール |
| :--- | :--- | :--- |
| LIF/ALIF ダイナミクス | `v ← αv + W_in·x + W_rec·z − ϑ·z`；`a ← ρa + z`；閾値 `ϑ + βa` | `neurons.ALIFCell` |
| 擬似微分 | `ψ = κ·max(0, 1−\|v_scaled\|)` | `neurons.pseudo_derivative` |
| Eq. (25) | 資格痕跡 `ε_v`、`ε_a`、`e = ψ(ε_v − βε_a)` | `traces.eligibility_traces` / `traces.eprop_gradient` |
| Eq. (28) | `ΔW_ij = −η Σ_t L_j^t e_ij^t` | `eprop.EPropLearner.update` |
| 学習信号 | `L_j^t = (W_out·∂E/∂y)_j^t` | `eprop.EPropLearner.learning_signal` |

## 「局所」の正確な表現

§9 の「大域的な逆伝播なし」は本実装では**検証可能**であり、文章上の約束ではありません：

* 前向きシミュレーションは `torch.no_grad()` で走り、スパイキングネットワーク内部に
  逆伝播できるグラフがありません；
* 資格痕跡は明示的な漸化式で計算され、微分では得ていません；
* 学習信号には**閉じた形**があります（`W_out` に softmax 誤差を掛ける）。「フィルタ後の
  スパイクまで微分する」という段階すら省いており、`eprop.py` 全体に `backward()` も
  `autograd.grad` も一度も現れません；
* 読み出し層は通常の勾配降下で更新します（単なる線形分類器であり、論文のやり方です）。

三つのテストがこれを見張っています：重みの変化は**手計算した** `−ηΣ_t L_j^t e_ij^t` に
一致しなければならない；更新後に `w_rec.grad is None` である；そして**対照**——同じ
アーキテクチャを BPTT ベースラインに替えると `.grad` は `None` ではなく、前向き計算は
グラフを構築しなければならない。片側だけ見ても「グラフを作らなかった」ことしか分かり
ませんが、両側を見れば違いがどこにあるかが分かります。

## 因子分解：厳密と近似は別のこと

e-prop の `Σ_t L_j^t e_ij^t` は BPTT の勾配では**ありません**。落とす経路は二系統です：
ニューロン間の循環経路、そして**リセット項** `−ϑ·z^{t−1}` に由来する自身のスパイク媒介
経路——後者は `W_rec` に依存しないため、循環重みをゼロにしても消えません。

このことは二つのテストで別々に押さえています：

* **厳密**：「落とす経路が無い」構成（`W_rec` をゼロ、不応期なし、損失は最終時刻のみに
  依存）では、因子分解と BPTT は**相対差 0.000e+00** で一致します。これは Eq. (25) の
  式と時間整列を固定します；
* **近似**：一般の場合、両者は一致せず、実測で 20〜40% です。これは e-prop の設計上の
  トレードオフであり、テストは差を**測定して**説明可能な範囲に収まることを主張します。
  満たしていない精度を主張するのではありません。

## 最小再現スクリプト

`examples/paper_bellec2020.py`（プロジェクト計画書 §12.3）。MNIST のダウンロードに依存せず、
合成の順序タスクで CPU 数十秒で終わり、CI が直接実行します。上記の「厳密に一致」をその場で
再現し、学習経路に autograd が無いことを主張します。

`examples/paper_pes2025.py` は **Pes 2025 の Trace Propagation** の最小再現です——同じく合成
タスク、同じく CPU で十数秒。**本項目で使う学習規則を検証するものではありません**（TP はそれでは
ない。[ADR-0010](../../docs/adr/ADR-0010-eprop-quadratic-storage-and-trace-propagation.ja.md) 参照）。
論文の規則そのものが動くことを検証し、あわせて論文の式と公式実装が食い違う箇所を列挙しています
（Eq. (1) のリセット項の off-by-one、Eq. (15) が前層の痕跡を使うこと、層をまたぐ detach が必須で
あること、出力層の更新式が論文に無いこと……）。

## 既知の境界

この節は結論より重要です。

1. **Trace Propagation は本項目では実装しません——これは考証の結論であり、負債ではありません。**
   §3.1「重要な修正 1」は資格痕跡の記憶を O(N) へ下げることを要求します。原典
   （arXiv:2509.13053）を確認した結果：
   - **計画書の前件は成立します。** Table 3 に独立した `E-prop [2]` 行があり、Space Complexity =
     `LH²` です。§1.3.1 は「シナプスごとに保存する」という族の性質の例として e-prop を名指し
     してもいます。したがって、その計算量を e-prop に帰属させるのは**拡張ではありません**
     （`docs/references.md` の以前の判断は撤回済み）。
   - **しかし「TP を採用する」という是正措置は該当しません。** TP は e-prop の省メモリ版では
     なく、**空間的信用割り当てを置き換える別の規則**です（論文は両者を別々の行・別々の軸で
     並べており、TP が e-prop の最適化だとは述べていません）。TP の結果は**すべて LIF** ですが、
     本項目の受け入れ設定は **ALIF（β=0.07）** です。しかも論文自身の N-MNIST で
     **e-prop 97.90 > TP 97.33 ± 0.06**——e-prop は `Partial (time)` 局所とされ、「完全局所」の
     比較集合にすら入っていません。裁定は `docs/adr/ADR-0010`。
   - **本実装が実際に行うべき唯一のことは実施済みです。** `epsilon_v` は
     `(batch, n_pre, n_post)` で確保されていましたが、その漸化式に**後シナプス添字 j は現れません**。
     `(batch, n_pre, 1)` にすると**ビット単位で同一**（`torch.equal=True`、`max|diff|=0`）で、
     受け入れ形状での実測壁時計が **−18.2%** になります。`epsilon_a` の係数には `(ρ − β·ψ_j)` が
     含まれ**因数分解できない**ため、メモリの大半は ALIF の適応項が決めます。LIF（β=0）では
     両方とも外積に退化し、痕跡状態全体が O(batch·N) まで下がります。
   - オンライン累積の段は依然として有効です：省いているのは**時間次元**
     （`O(T·batch·N²)` → `O(batch·N²)`）で、実測では「n_rec=256 で 1 エポックも終わらない」から
     50 s/エポックになりました。
2. **これは 30 エポックの結果で、収束していません。** 最終エポックでも検証精度は上昇中でした。
3. **複数シードの統計ができました（5 シード）——ただし「論文より良い」にはまだ
   遠いです。** W1 と同じ口径です
   （`uv run python -m research.eprop.sweep_seeds --seeds 0-4 --device cuda`）：

   | シード | テスト正確率 | 活動ニューロン比率 |
   | ---: | ---: | ---: |
   | 0 | 0.7748 | 1.0000 |
   | 1 | 0.7678 | 1.0000 |
   | 2 | 0.7789 | 1.0000 |
   | 3 | 0.7857 | 1.0000 |
   | 4 | 0.7746 | 1.0000 |
   | **平均** | **0.7764** | **1.0000** |
   | 最小 / 最大 | 0.7678 / 0.7857 | 1.0000 / 1.0000 |
   | **最大最小差** | **1.79 パーセントポイント** | **0.00 パーセントポイント** |

   ⚠️ **活動比率の列にはシード分散がまったくありません**（5 シードすべて 1.0000）
   ——§七 の 60% しきい値から遠く、**識別力のある指標ではありません**。計画書が求めているため
   報告しているだけです。
   ⚠️ **正確率の側で重要なのは最大最小差ではなく、収束していないことです**：検証正確率は
   30 エポックでも上昇中で（曲線は
   [`figures/f2-w2-epoch-accuracy.png`](../../figures/f2-w2-epoch-accuracy.png)）、
   0.7764 は**予算で打ち切られた**値であって、この規則の sMNIST における上限では
   ありません。論文を超えられるかはエポック予算を広げてから問う話で、**達成済みの結論では
   ありません**。

   シードごとの JSON と完全な stdout（5 件の再現記録ブロック、`Git 状態` はすべて
   「クリーン」）は [`sweep_results/w2_seeds/`](../../sweep_results/w2_seeds/) と
   [`sweep_results/w2_seeds.log`](../../sweep_results/w2_seeds.log) にあります。
   もう一つ、**独立に再現したクロスチェック**のバッチ（同一設定、エポックごとの記録を足す前に
   実行）が [`sweep_results/w2_seeds_nocurve/`](../../sweep_results/w2_seeds_nocurve/) にあり、
   その平均は **0.7764**（最小 0.7678、最大 0.7857）で、正式バッチの
   0.7764 と 0.00 パーセントポイントしか違いません——
   二回の独立実行が互いに一致しています。

   ⚠️ 本項目は以前「**単一の実行であり、複数回の平均ではありません。** シードは `base=0` の
   一つだけです」と書かれていました。その記述はこのバッチに置き換えられました。
4. **SHD の傍証は実行しましたが、学習しませんでした**（上の「SHD の傍証」参照）。この項目は
   かつて「未実行」と書かれていました——実際に走らせたところ、`load_shd` に**実行して初めて
   露見する**二つの欠陥が見つかり（可変長データをファイルを閉じた後に添字付けしていたこと、
   ミリ秒でビニングして先頭 100 ms しか取っていなかったこと）、修正して再実行した結果が
   「**同じハイパーパラメータでは学習しない**」でした。したがってこれは欠落ではなく負の結果で
   あり、「SHD 向けの調整」は本フェーズでは行っていません。
5. **入力符号化は本プロジェクト自身の選択です。** MNIST のアナログ画素をベルヌーイ
   サンプリングでスパイクにします。どの論文もこれを規定しておらず、符号化を変えれば結果も
   変わります。
6. **BPTT とのギャップの定量報告：同一予算で 23.3 パーセントポイント差。**
   `bptt_baseline.py`（同一アーキテクチャ、同一損失、同一予算、同一シード。違いは学習規則
   だけ）：

   | アーム | 学習規則 | テスト正確率 |
   | :--- | :--- | ---: |
   | e-prop | 局所適格性痕跡、**層をまたぐ勾配なし** | **0.5274** |
   | BPTT | 代理勾配でスパイクを通過、計算グラフを切らない | **0.7602** |
   | | **ギャップ** | **+0.2328** |

   設定 `n_rec=256, epochs=10, n_train=20000, batch=64, seed=0`。生の stdout（再現記録ブロックを含む）は
   [`sweep_results/w2_bptt.log`](../../sweep_results/w2_bptt.log) にあります。

   **誤読を避けるため、先に三つの口径を述べます**：
   - **二つのアームで学習率が異なります**（e-prop 2e-3、BPTT 5e-3——それぞれのスクリプトの
     既定値）。したがってこれは「各自の既定学習率でのギャップ」であり、**ハイパーパラメータを
     揃えた後のギャップではありません**——一部は学習率の違いにすぎない可能性があります。
   - **この予算では e-prop は収束していません**（10 エポック × 2 万サンプル）。23.3
     パーセントポイントの大半は「収束が遅い」であって「上限が低い」ではありません。
   - **予算をまたいで見ると、目を引くのは計算効率です**（これは**対比較ではありません**——
     予算が違います）：BPTT は **200 万**回のサンプル提示で 0.7602 に
     達しますが、受入設定の e-prop は**約 16200 万**回（30 エポック ×
     5.4 万。5.4 万 = 6 万の訓練集合から 10% の検証集合を引いたもの）でようやく 0.7748
     です——**約 8 倍の計算量で 1〜2 ポイント**。これが本報告の本当の情報量であり、
     「e-prop は遅い」を印象から引用可能な数値に変え、§九 フェーズ 4 の目標「ギャップの
     縮小または明確な帰因分析」の出発点になります。

   ⚠️ 本項目は以前「**BPTT とのギャップの定量報告はまだ出していません**……数値はまだ存在
   しません」と書かれていました。その記述は本報告に置き換えられました。
7. **隠れ層は一つです。** `EPropLearner` は ALIF 一层 + 線形読み出しです。§2.2 の認知コアは
   多層 RSNN であり、層の積み重ねは未実装です。
