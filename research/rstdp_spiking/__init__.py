"""脉冲版执行层（W3 的第三个阶段任务）—— **前期零件，第一阶段不推进**。

**⚠️ 破坏性变更免责期：至 2027-09**（计划书 §12.5、ADR-0005）。

## 这个目录现在是什么状态

它是**第三阶段任务「执行层脉冲化」的前期零件**，不是一条已完成的验证线：

| 有 | 没有 |
| :--- | :--- |
| `neurons.py` LIF 层 | 群体 Critic（TD-LTP） |
| `plasticity.py` **带真实 Δt 的成对 STDP** + 资格痕迹 | CartPole 训练/评测入口 |
| `encoding.py` 群体编码 + 脉冲编码 | 任何**训练结果**——本目录跑不出「多少步」这个数 |
| `actor.py` 脉冲 R-STDP Actor | README（按讨论，随第三阶段一起写） |
| `tests/` 31 条，全过 | |

**计划书 §七 第三阶段**写着这条任务与它的评测协议（**冻结随机流**），见修订记录 ⑪。
第一阶段到此为止，`research/rstdp/` 的速率型实现仍然是唯一的验收对象。

## 为什么留着它而不是等第三阶段从头写

`plasticity.py` 把**速率版结构上做不到的那件事**做出来了：`STDP(Δt)` 的窗口形状。
`research/rstdp/README.md` 逐字承认「TD-LTP 与 TD-STDP 那处『有无 post-before-pre 分量』的
差别在本实现里无从体现」——本目录的测试**逐 Δt 对着 `exp(−Δt/τ)` 断言**，并有一条专门
钉住「改 τ₊ 结果必须变」。第三阶段接手时不必重新论证这件事成不成立。

## 与 `research/rstdp/` 的关系

**并列，不是替换。** 速率版保留为对照基线：同一任务、同一规则、脉冲 vs 速率——这本身
就是一条有信息量的实验（它回答「脉冲化之后性能掉多少」）。

两个主题之间**互不 import**（仓库约定：三条线只共享 `research/common/`）。
"""

__all__: list[str] = []
