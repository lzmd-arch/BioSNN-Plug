# W3：R-STDP 执行层 + TD-LTP Critic

**⚠️ 破坏性变更免责期：至 2027-09**（计划书 §12.5、ADR-0005）。

## 在验证什么命题

计划书 §3.2 的执行层：**带 TD-LTP Critic 的 R-STDP 能否在 CartPole 上稳定学到 200 步
以上，且成功偏移 < 10%σR。**

Critic 不是可选项。§3.2 引 Frémaux et al. (2010)：成功偏移达 σR 的约 25% 就无法学习，
偏移 < −0.4σR 时性能甚至低于学习前。该敏感性是通用特性，调 STDP 窗口或换权重依赖模型
都解决不了；唯一的结构性解法是刺激特异性奖励预测。

## 当前结论

**仍未达到验收。** ≥10 个种子的中位数 **100.0 步**（阈值 ≥ 200），最小 63、最大 173。
说得更完整一点：**本阶段试过的每一条杠杆都没有通过预登记的采纳规则**——而失效模式已经被
测清楚到可以逐条否证的程度。

### 采纳规则（先于测量写死）

逐种子配对差值**中位数 > 0 且 ≥7/10 个种子变好**。基线是 `actor_lr_final_fraction=0.1`
（10 种子中位数 100.0）。多随机种子是必需的：同一配置内种子间实测能差 5–30 倍，
单种子结论在这个问题上不可用。

### 试过什么，结果如何

| 杠杆 | 配对中位数 | 变好 | 判定 |
| :--- | ---: | ---: | :--- |
| **Actor 学习率 3e-3 → 1e-3** | — | — | 有效，但只到 93.7 |
| Actor 学习率 3e-4 / 1e-4 / 3e-5 / 1e-5 / 3e-6 | −26 … −65 | ≤4/10 | 无效 |
| 编码宽度 σ ≥ 1.0 | — | — | 灾难（中位数 9.4） |
| 训练回合数 2400 / 400 / 200 / 100 | −23 … −55 | ≤3/10 | 无效（越少越差） |
| 关掉 Actor 的 L1 归一化（等尺度补测） | −1.6 | 5/10 | 无效 |
| 痕迹第二因子改成 softmax | −88% | 0/5 | 灾难 |
| 痕迹第二因子改成 `a_j − 1/n` | — | — | 按构造无效（行公共向量，argmax 不变） |
| Critic 学习率 2e-3 / 8e-3 / 3e-2 | −69 … −66 | **0/10** | 灾难 |
| Critic 学习率 1e-4 | −2.4 | 5/10 | 无效 |
| Critic 单元数 256 / 1024 | −3.9 / +2.2 | 3/10 / 5/10 | 无效 |
| Critic 增益 4；阈值 0.6 / 0.8 / 1.0 | −37 … +6 | ≤5/10 | 无效 |
| Critic 输出偏置（可学 / 固定 −20） | −6.3 / +0.3 | 4/10 / 5/10 | 无效 |
| Critic 半梯度后因子 `y(1−y)` | −7.2 | 5/10 | 无效 |
| Actor 成功信号裁剪 | — | — | **网格选错**：三档全在 σR≈4.8 之下，等于又扫了一遍 η |
| Actor Polyak 参数平均 τ=0.99 | −25.9 | 2/10 | 无效（游走被压平，好解也一起没了） |
| 行为策略换 Boltzmann + 痕迹中心化 | −72 … +8 | ≤6/10 | 无效 |
| Critic 固定**有符号**读出 + 半梯度 | −50.7 | **0/10** | 无效——但机制上有效，见下 |

### 两条决定性的测量

**一、策略在全程游走，终值只是它停在哪一相的一个采样。** 每 50 回合评一次贪心策略：

```text
frac=1.0 seed 3:  78 374 176 38 39 12 9 12 96 83 18 39 94 47 22 9
frac=0.1 seed 0:  24 30 29 32 11 10 24 21 32 94 92 428 226 130 111 61
frac=0.1 seed 9:  12 9 9 56 10 34 19 40 54 37 31 28 31 21 244 88
```

二十条曲线**全部**如此，相邻检查点相差 5–30 倍。所以「最终权重的 10 个贪心回合均值」这个
口径量到的很大程度是**相位**而不是水平。峰值中位数 158.0——**也够不到 200**。

**二、把 Critic 的误差真的降下来，策略反而更差。** 预登记的分叉规则说「EV < 0.5 ⇒ 瓶颈在
Critic」。这个推断**被它自己的干预否证了**，见下一节。

### Critic 学不准，原因已经定位到读出

把 Actor 学习率设为 0 就得到**完全静止**的目标，非平稳这条解释被排除——EV(V, V\*) 仍是
−1.00 / −0.51 / −0.81。也**不是步长**：Critic 学习率往下调，EV 单调变差
（5e-4 → 1e-4 → 2e-5 对应 −0.75 → −1.30 → −6.45）。

把固定读出换成**最小二乘解**、其余一律不动，**同一批单元活动**的 EV 从 −0.9 变成 **+0.6**：
特征里有信息，是「固定、全正、均匀」这个读出把它扔了。（对照：**未训练**的随机方向已经有
+0.53，所以训练单元方向这件事本身收益极小。）

于是加了 `critic_readout="random"`（固定但**有符号**）配完整的半梯度后因子
`u_j·g·y_j(1−y_j)`。**机制上确实有效**：EV −1.00 → −0.21，平均绝对误差 10.8 → 5.4。

**但步数 0/10 变好**（配对 −50.7）。原因是一个量得出来的混淆：有符号读出让 `V = Σu_j y_j`
变成正负相消的差，**σR 从 4.30 掉到 1.40**，等于把标定过的 Actor 有效步长压小了 3 倍。

**步长补偿的对照已经跑完，结论不变。** 把 Actor 学习率按 σR 的比例放大到 9e-3
（`3e-3 × 4.30 ≈ 9e-3 × 1.40`，乘积即有效步长），配对数仍是 **−40.6、4/10 变好**，
中位数 45.9。**所以「把 δ 变准并不能把策略变好」这一条在补偿之后依然成立**——
预登记的分叉推断（EV < 0.5 ⇒ 瓶颈在 Critic）就是这样被它自己的干预否证的。

### 已经确立的（不再是假设）

- **Actor 的规则方向是对的，量级不对。** 用精确 rollout 算出优势后，规则自己的平均更新
  方向与「精确优势加权方向」的余弦是 **cos = 0.74**（逐种子 0.65–0.91），而步长比
  **|U|/|T| = 20–36**。
- **规则不做动作之间的比较。** `learn_step` 只在非探索步更新 Actor，而那些步的
  `action = argmax` 是状态的确定性函数，所以它只强化**当时选的那个**动作。这也是为什么
  按 `a_j − π_j` 中心化**单独做会把 Actor 完全停掉**（`a_j − π_j ≡ 0`）——代码里有断言挡着。
- **Boltzmann + 中心化无效，而且有结构性原因**：中心化后的更新量正比于 `a − π`，策略越尖锐
  它越小。logit 80（接近确定性）直接崩到 12.4 步。
- **容量不是瓶颈**：同一编码上监督拟合的线性策略跑 497 步（见下一节）。
- **Critic 的值域不是瓶颈**：V 的宽度是 V* 的 0.73–0.84，够宽；错的是形状。

### 本阶段修掉的四个真缺陷

1. **截断被当成终止。** 跑满 500 步的回合最后一步拿到 `δ ≈ 1 − 99.3 = −98`，约 56 倍典型
   权重，而且**策略越好打得越狠**——这是「峰值 500 → 崩到 14」最直接的解释。修完后
   不衰减那一档的前 5 个种子与修复前**逐位相同**，说明改动精确落在截断路径上。
2. **观测污染了被观测。** 中途贪心评测推进了训练环境自己的 RNG 流，改掉后面每个训练回合的
   初始状态（同配置同种子 seed 6 终值 107.3 → 335.4）。改成用另一个环境实例后逐位一致。
3. **Critic 偏好方向的采样池给了 `critic_units` 一个隐藏上限**（约 850–960），
   `critic_units=1024` 时十个种子全挂。
4. **我自己写错的一条符号论证。** 「读出必须全正」只在 `rate` 后因子下成立；换成 `gradient`
   后因子后，符号由 `u_j` 自己带进更新方向。

### 改动前（单单元 Critic，作为对照保留）

`--critic-kind single` 可复现改动前的行为：

| seed | 步数 | \|偏移\|/σR |
| ---: | ---: | ---: |
| 0 | 9.3 | 0.9676 ✗ |
| 1 | 23.1 | 0.4364 ✗ |
| 2 | 37.3 | **0.0530** ✓ |
| 3 | 9.4 | 0.7571 ✗ |
| 4 | 9.5 | 1.3705 ✗ |

五个种子没有一个达到 200 步；随机策略在 CartPole 上约 9–10 步。**偏移大的种子恰好是学不动
的种子**（0.97 / 0.76 / 1.37 → 9 步）——方向与 §3.2 那条断言一致，但没有复现出定量的阈值边界。

复现记录（`--seed 0`，由 `research/common/provenance.py` 生成）：

```text
实验名称：rstdp/cartpole
日期：2026-09-23 03:05:37 中国标准时间
Git commit：5e64247ef9bc08931b225ba8ae100f3968dacc9f
Git 状态：干净
Python：3.12.14
操作系统 / 架构：Windows 11 / AMD64
硬件：CPU（Intel64 Family 6 Model 183 Stepping 1, GenuineIntel，无 GPU 参与）
随机种子：base=0；Actor 与 Critic 初始权重=3589114572；Critic 感受野采样=1786091376；动作探索=3138835151；感受野采样环境=2269638270；环境动作空间种子=4106697854；环境随机种子=2726622797；群体编码中心=4160090208
依赖快照：uv.lock sha256=cbafb161e71421f9f228e23e2ab6f4f742f899e9958d290f214dac1dd948e37f
运行命令：cartpole.py --seed 0 --device cpu
耗时：27.6 s
显存峰值：0.0 MiB（§6.2 硬约束 8GB）
§6.2 降级路径：未触发
备注：N=64, sigma=0.5, eta_actor=0.003, eta_critic=0.0005, trace_decay=0.9, gamma=0.99, success_signal=td_error
备注：Actor：normalize=True, clip=None, polyak_tau=None, sampling=epsilon_greedy, logit_scale=1.0, trace_center=none, lr_final_fraction=1.0
备注：Critic：kind=population, units=64, value_scale=200.0, gain=8.0, threshold=0.4, output_bias=0.0, bias_lr=None, post_factor=rate
备注：探索：start=0.3, end=0.02, 回合数=800
备注：状态编码：4 维连续状态的高斯群体编码（本项目自己的选择）
备注：速率型单元：STDP 窗口形状与 TD-LTP/TD-STDP 的差别在此化简下无从体现
```

## 怎么跑出来

```bash
uv run python -m research.rstdp.cartpole              # 验收运行（约 4 秒）
uv run python -m research.rstdp.cartpole --seed 2     # 换种子
uv run python -m research.rstdp.cartpole --smoke      # CI 冒烟
```

## 方程到模块的对应

| 论文 | 内容 | 模块 |
| :--- | :--- | :--- |
| Frémaux 2013 Eq. 17 | TD-LTP：`Δw = η·δ·e`，`e ← λe + x·y` | `td_ltp.TDLCritic` |
| §3.2 | R-STDP：`Δw = η·S·e`，`e ← λe + x·a` | `rstdp.RSTDPActor` |
| §3.2 | 权重归一化 | 两个类各自实现 |
| §3.2 / §七 | 成功偏移 / σR | `measure_bias` |

## 实现过程中修掉的三个结构性 bug

每一个都配了回归测试，且都不是"调参能绕过"的。

**1. Critic 的零点锁死。** 资格痕迹是 `e ← λe + x·V`，被 Critic **自己的输出**门控。
最初用零初始化 → `V ≡ 0` → 痕迹恒为 0 → 更新量 `η·δ·e` 恒为 0 → Critic 永远学不动。
这是三因子结构里"后突触活动作为第二因子"带来的锁死。

**2. 正值反馈导致发散。** 改成小幅随机初始化后能动了，但学习率一调高权重就发散成 NaN：
`V` 变大 → `e = x·V` 变大 → `Δw` 变大 → `V` 更大。

**3. 缺了 Critic 的权重归一化。** §3.2 明写「同时引入权重归一化：每个神经元层面保持
权重总和恒定，防止突触动态失控」——我当初只给 Actor 加了。补上 L2 归一化后回路断开，
CartPole 上从 9.5 步升到 84 步。另配 `value_scale` 恢复被归一化拿掉的整体增益。

另修两处**测量/复现**缺陷：进度日志里的痕迹列打印时机错了（`end_episode()` 之后取，
恒为 0）；以及 **CartPole 环境从未播种**——同一 seed 连跑两次得到 20.4 步与 9.3 步。
后者对结论尤其要紧：修复之前，"种子间差异"里有多少来自算法、多少来自环境 RNG 是分不清的。

## 容量探针：一个否定性结果

`research/rstdp/capacity_probe.py` 用 CartPole 的经典启发式控制器当靶子，测**这个编码上
线性策略的容量上限**——线性分类器的形式与 `RSTDPActor` 的动作选择完全一致。

| 特征数 N | σ=0.25 | σ=0.5 | σ=1.0 |
| ---: | ---: | ---: | ---: |
| 16 | 24.6 | 209.8 | 494.2 |
| 64 | 294.2 | **497.2** | 500.0 |
| 256 | 449.4 | 500.0 | 500.0 |
| 1024 | 498.4 | 500.0 | 500.0 |

（表内是拟合出的线性策略的平均存活步数；专家启发式本身是 500.0。）

**当前配置就是 N=64、σ=0.5，对应 497.2 步。** 也就是说：这个编码上**存在**一个几乎解出
CartPole 的线性策略，而 R-STDP 没找到它。

所以「Actor 容量不足」这个假设**被否定**，改学习规则之外的方向（加容量）不是答案。这也
排除了一个原本看起来最省事的解释。

## 改动前后对比：为什么换群体结构

单单元 Critic 把"单元输出"与"值"当成了同一个量，于是它的资格痕迹是 `e = x·V`。这带来三个
连锁问题，实测都出现过：

| 问题 | 症状 | 群体结构为什么治得了 |
| :--- | :--- | :--- |
| 零点锁死 | 零初始化下 `V ≡ 0` → 痕迹恒 0 → 永远学不动 | `y_j = σ(…)` 在 0 处是 0.5，不是 0 |
| 正值反馈 | `V`↑ → `e=x·V`↑ → `Δw`↑ → `V`↑，权重发散成 NaN | 第二因子是 `y_j ∈ (0,1)`，**有界** |
| 尺度自由度被夺走 | 为堵上一条加了 `‖w‖=1`，于是 `V` 顶到上界、`δ` 变常数 | 尺度由**固定读出**承担，`w_j` 只管形状 |

改动之后的数字见上一节：偏移 1/5 → 5/5，步数 9–37 → 60–120。**这条改动值不值得做，看这两个
数字就够了**，不需要再去猜哪个超参更敏感。

## 已知边界

1. **验收未通过。** ≥10 个种子的中位数 100.0 步，阈值是 200。本阶段试过的每一条杠杆都记在
   上面那张表里，**没有一条通过预登记的采纳规则**。
2. **成功信号换了默认值。** §3.2 写 `S = R − ⟨R⟩`，但 CartPole 每步都给 +1 的**密集恒定
   奖励**，`⟨R⟩ ≡ 1`，于是 `S ≈ 0`、毫无对比度——那个式子是给**稀疏终末奖励**的设定写的
   （Frémaux 2010 的原始场景）。默认改用 TD 误差，两种都保留可选。所以本条目验的**不是**
   计划书字面上的那个成功信号。
3. **速率型而非脉冲型。** 速率型下没有"脉冲先后"，所以 `STDP(Δt)` 的窗口形状退化成同时刻的
   乘积，**TD-LTP 与 TD-STDP 那处"有无 post-before-pre 分量"的差别在本实现里无从体现**。
   ADR-0007 的备选 C（退回 TD-STDP）因此不是本实现里的一个开关——真要退需要先改回脉冲型。
   **没有尝试过那个回退。**
4. **策略不收敛，这是最要紧的一条边界。** 每 50 回合评一次的曲线显示二十条轨迹**全部**在
   全程大幅震荡（相邻检查点差 5–30 倍）。所以「最终权重在 10 个贪心回合上的均值」这个验收
   口径量到的很大程度是**相位**而不是水平；**峰值中位数 158.0，也够不到 200**。任何只报
   一个终值的结论在这里都要打折看。
5. **「Actor 容量不足」已被否定。** 在**同一个编码**上，一个监督拟合的线性策略能跑 497 步。
6. **把 Critic 变准并不改善步数——这条否掉了本阶段预登记的分叉推断。** 分叉规则说
   「EV < 0.5 ⇒ 瓶颈在 Critic」，实测确实 EV < 0（−1.0 … −2.0），于是我去修 Critic：
   换固定有符号读出 + 完整半梯度后因子，**EV −1.00 → −0.21、平均绝对误差 10.8 → 5.4**，
   机制上成功了。**结果步数 0/10 个种子变好**（配对中位数 −50.7）。
   **混淆已量出并已补偿**：有符号读出让 `σR` 从 4.30 掉到 1.40（Actor 有效步长被压小 3 倍）；
   把 Actor 学习率按同一比例放大到 9e-3 之后，配对中位数仍是 **−40.6、4/10 变好**。
   **所以「先把 Critic 修准」这条路已经被排除，不是「还没试」。**
7. **Actor 的规则方向对、量级不对，而且不做动作之间的比较。** 用精确 rollout 算优势后，
   规则自己的平均更新方向与精确优势加权方向的余弦是 0.74，而步长比是 20–36 倍。另外
   `learn_step` 只在非探索步更新 Actor，那些步的 `action = argmax` 是状态的确定性函数，
   所以规则只强化当时选的那个动作。**换成 Boltzmann + `a_j − π_j` 中心化是无效的**，且有一条
   结构性原因：中心化后的更新量正比于 `a − π`，策略越尖锐它越小（logit 80 崩到 12.4 步）。
8. **调参必须在环境播种之后重做。** 更早那批"超参敏感"的结论（例如「Critic 学习率 1e-3 与
   5e-4 差 44 步 vs 223 步」）**是在环境播种之前测的，因而不可信**——那时同一配置连跑两次
   就能差一倍。本条目此前的调参判断大部分已作废。
9. **一处网格设计错误记在这里。** Actor 成功信号裁剪那三档取的是 {0.3, 1.0, 3.0}，而实测
   `σR` 的中位数是 4.78——三档**全部低于信号自身的标准差**，于是那一轮实际扫的是「更小的
   有效学习率」，而不是「削掉极端尾巴」。要测后者，阈值得在 `σR` 之上。
10. **单单元 Critic 的两个病已定位并修复，但那只解释了改动前的一部分。** 它的资格痕迹被自己
    的输出门控（零点锁死）、更新量正比于 `V`（正值反馈 → 发散）、为堵上一条加的归一化又夺走
    了它的尺度自由度（`V` 顶到上界 → `δ` 变常数）。换成群体后这三个都消失。但把单单元版换成
    标准 TD(λ) 也只到 21–40 步——说明那时还有别的因素在起作用，**而那些因素现在大概率还在**。
11. **§3.2 那条定量边界未复现。** 「偏移达 ~25%σR 就无法学习」需要设计一个动作分布保持混合的
    任务才能干净地量出来；`examples/paper_fremaux2013.py` 刻意**不**演示它，因为我第一版写的
    演示量的其实是"actor 恰好选哪个动作"，不是偏差结构。
12. **种子方差极大，且它是动力学的而非统计的。** CartPole 的动力学是确定性的，随机性只来自
    初始状态、探索与初始权重。10 个种子的中位数仍有很宽的置信区间，所以此处的每一个"达标"
    或"不达标"都按 ≥10 种子报，并**必报最小值与最大值**。
13. **CPU 与 CUDA 的数字不可比**（浮点与随机流不同），所有扫描都固定在 CPU 上跑。

### 还没试的结构性方向

按证据强度排序，都仍在「无代理梯度、纯局部」之内：

1. **按动作拆开 Critic**（两群单元给 `Q(s,0)`、`Q(s,1)`，用 `A = Q − V` 驱动 Actor）。单个
   **状态值** Critic 只能给出状态级的优势，而 CartPole 的失败是动作特异的。这是经典解法，
   也是本条目结构上唯一还没动过的一环。**但注意边界第 6 条**：把 Critic 变准这条路已经被
   否证过一次，所以这一条的先验要相应调低。
2. **让局部可塑性塑造编码本身**（现在是固定随机编码 + 线性读出）。容量探针给的是这个组合的
   上限（497 步）够高，但学习规则可能够不到。
3. **刺激特异性奖励预测**——§3.2 点名的结构性解法：把「一个全局 δ」换成按刺激通道分组的预测。
4. **Trace Propagation**（Pes 2025）——计划书 §七 认知线引的机制，本阶段尚未实现。
