# 实验产物（原始数据）

**这个目录进版本库**（`.gitignore` 里曾经忽略它，2026-09-23 改成入库）——原始实验产物是后面写论文时
画图、做表的唯一来源，只留在某一块硬盘上不叫"保留"。

**保留策略**：

1. **不删除、不覆盖**。新实验**新开目录**，不改动已有目录里的文件——即使那次实验的结论后来被否证
   （例如 `w1_reaccept` 那次因为中途改了代码而作废），也原样留着并在下面注明。
2. 一次运行 = 一个 `.json`（`sweep.py` 的产物）；一次扫描 = 一个目录。
   **汇总 `.log` 不一定与目录同名**：调参期的批次日志一次跑好几个分支、结果分散到好几个目录
   （见下面「批次日志」一节），所以按名字找 log 会漏。`sweep_results/` 里每个入库的 `.log`
   都在本文件里有交代——这是这份清单要保证的事。
3. **数据集不在这里**。`data/`（MNIST、SHD，共约 557 MB）按计划书 §12.1 不入库，复现方式是
   `uv run python scripts/download_data.py <名称>`（逐文件校验 + 打印 SHA-256）。

## 怎么读一个 json

一次运行的全部产物，键名与含义：

| 键 | 含义 |
| :--- | :--- |
| `seed` | 种子 |
| `mean_steps` | **验收口径**：最终权重上 10 个贪心回合的平均步数 |
| `min_steps` / `max_steps` | 那 10 个回合的最小/最大步数 |
| `offset` / `sigma` / `offset_ratio` | 成功偏移、成功信号标准差、两者之比（§七 判据 < 0.10） |
| `elapsed_s` | 墙钟（秒） |
| `peak_mb` | 显存峰值（CPU 跑时为 0） |
| `peak_mean_steps` / `peak_episode` | 训练途中贪心评测的最好值（**只作诊断**，验收不看它） |
| `curve` | 每 50 回合的评测点——**论文画曲线用的就是它** |
| `config` | 这次运行的完整配置（与文件名里那串对应） |

## 目录清单

### W3（R-STDP + TD-LTP Critic）的调参序列

按时间顺序，**每一个目录对应 README 里的一段结论**：

| 目录 | 配置要点 | 种子 | 它与 README 的关系 |
| :--- | :--- | :--- | :--- |
| `step0` | 基线（`lr_final_fraction=0.1`） | 0–9（20 跑） | 「采纳规则」里的基线中位数 100.0 |
| `step2_eta` | 扫 Actor 学习率（1e-1 … 1e-5） | 0–9（70 跑） | 「试过什么」表的 Actor 学习率那几行 |
| `step2_curve` / `step2_curve_fixed` | 每 50 回合的贪心曲线 | 0–9 | 「策略在全程游走」一节；`_fixed` 是修掉「观测污染被观测」之后的版本 |
| `step3_clip` / `step3_bias_fixed` / `step3_bias_lr` / `step3_critic_lr` / `step3_polyak` | 五个杠杆：成功信号裁剪、固定输出偏置、偏置学习率、Critic 学习率、Polyak 平均 | 0–9 | 「试过什么」表对应各行（含那条**网格选错**的裁剪，边界第 9 条） |
| `step4_*`（`units`/`units1024`/`gain`/`threshold`/`postfactor`/`critic_lr_low`） | 群体 Critic 的结构扫描 | 0–9 | 「Critic 学不准，原因已经定位到读出」一节 |
| `step5_*`（`center5`/`center20`/`center80`/`boltz_nocenter`/`budget`） | Boltzmann 行为策略与痕迹中心化 | 0–9 | 「采纳规则」那两行 |
| `step6_readout` / `step6_readout_scaled` | 固定**有符号**读出 + 完整半梯度（后者把 Actor 步长按比例放大 3 倍作补偿） | 0–9 | 边界第 6 条（把 Critic 变准并不改善步数） |
| `step7_boltz_greedy` | 修掉「贪心评测其实是采样」之后的**重测** | 0–9 | 「本阶段修掉的五个真缺陷」第 5 条 |
| `step8_heldout` | **留出种子**：常量 logit 20 | **10–24**（15 跑） | **中位数 177.3、最小 10、最大 434**（README 与 ADR-0009 都引用） |
| `step9_anneal` | 逆温度退火（起点 2）扫描 | 0–9（40 跑） | 「逆温度退火」那几行 |
| `step10_heldout_anneal` | **种子 25–44**：退火 2→20 / 2→40，学习率末期 0.1 | 25–44 | **260.0 / 最小 74**（ADR-0009 正文的数值） |
| `step11_start1` | 退火**起点** 1 与 2 的对比 | 25–44 | —— |
| `step12_lr003` | 学习率末期 0.03 | 25–44 | —— |
| `step13_lr001` | **最终配置**：退火 1→40、学习率末期 0.01 | 25–44 | **319.8 / 最小 109 / 峰值 441.7**（README「当前结论」的调参批次） |
| `step14_confirm` | **同一最终配置、全新种子** | **45–64** | ✅ **237.8 / 最小 121 / 峰值 389.8 —— 这就是验收数字** |
| `probe_ab` / `probe_curve_effect` | 两个诊断探针 | 0 | 「试过什么」里的「被评测缺陷污染」相关分析 |

### W1 / W2 的验收运行（含复现记录块的原始 stdout）

| 目录 | 内容 | 用途 |
| :--- | :--- | :--- |
| `w1_reaccept/out.log` | W1 第一次重跑 | ⚠️ **作废但留档**：那次运行中途我改了代码，工作区不干净，按仓库规矩结论不可信。留着是为了"哪一次不算数"也有据可查 |
| `w1_final/out.log` | **W1 最终验收运行**（干净工作区，0.9779） | `research/ib_hebbian/README` 的复现记录块逐字出自它 |
| `w2_shd/out.log` | **W2 的 SHD 旁证运行**（负面结果 0.0534） | `research/eprop/README` 的「旁证 SHD」一节逐字出自它 |
| `w3_reseed/seed0.log` | W3 默认配置 `--seed 0`（新默认退火 1→40） | W3 README 里那条 seed-0 复现记录 |
| `w3_reseed/seed45.log` | `--seed 45`（验证「默认即验收配置」：232.1） | ADR-0009 复核注记里的那条逐字一致 |
| `w3_reseed/seed0_single.log` | `--seed 0 --critic-kind single`（9.3 步） | W3 README 「单单元 Critic」那张表配的记录 |
| `w1_seeds/` + `w1_seeds.log` | **W1 的多随机种子扫描**（种子 0–4，论文的 5 种子口径） | W1 README 的「多随机种子」表与边界第 2 条逐字出自它：均值 **0.9774**、极差 **0.14** 个百分点 |
| `w1_ablation/` + `w1_ablation.log` | **W1 的消融对照**（5 条臂 × 种子 0）。每条臂对应论文 Table 4/Table 3 里 MNIST 的某一列，映射见 `research/ib_hebbian/ablation.py` | W1 README 的已知边界第 5 条逐字出自它。两处值得记：① `baseline` 臂是空覆盖，跑出 **0.9779**，与验收配置逐字一致；② `divnorm_off` 臂**发散了**——局部目标第 10 轮触底后一路上爬，所以那个 0.9261 **不能当作一次有效读数**，「那条列没复现」的结论就是从这个 log 的逐轮目标读出来的 |
| `w2_seeds/` + `w2_seeds.log` | **W2 的多随机种子扫描**（种子 0–4，sMNIST）。每个 json 另外带 `curve`（逐 epoch 验证准确率） | W2 README 的「已知边界」第 3 条出自它：均值 **0.7764**、极差 **1.79** 个百分点。`figures/f2-w2-epoch-accuracy.png` 里**红色那条 30 epoch 曲线**（虚线、空心圆、止于第 30 轮）也出自它 |
| `w2_seeds_100ep/` + `w2_seeds_100ep.log` | **W2 的 100 epoch 追加批**——同一批种子 0–4、同一份代码，**只把 epoch 预算从 30 放到 100**。每个 json 带 100 个 `curve` 点 | 与 `w2_seeds/` **逐种子配对**：截断的代价均值 **+7.55** 个百分点（最小 +6.05、最大 +8.66，5 对）。**前 30 轮与 30 epoch 批逐点相同**（5/5 种子 × 30/30 轮，两次独立运行），所以 30 epoch 的数字是这条轨迹的**前缀**，不是另一个结果——`figures/f2-w2-epoch-accuracy.png` 里**蓝色那条 100 epoch 曲线**与图上的数字框都出自它。⚠️ 到第 100 轮**仍未收敛**（最后 20 轮**窗口均值**的增量在 5 个种子上全为正：+0.0084 … +0.0147。判据取窗口均值而不是「最后一轮的增量」——后者是噪声，跨种子均值曲线第 100 轮是 −0.0003），所以 100 epoch 也**仍是预算截断的结果** |
| `w2_seeds_nocurve/` + `w2_seeds_nocurve.log` | **同一配置的独立复现批**（跑在加逐 epoch 记录之前，所以**没有** `curve` 字段） | 交叉核对：均值 **0.7764**，与正式那批差 0.00 个百分点 |
| `w2_bptt.log` | **e-prop vs BPTT 的量化差距报告**（同架构/同损失/同预算/同 seed，唯一差别是学习规则）。**只有日志、没有 json** —— `bptt_baseline.py` 只打印 | W2 README 已知边界第 6 条逐字出自它：e-prop **0.5274** vs BPTT **0.7602**，差距 **+0.2328** |

### 批次日志（一个 `.log` 覆盖多个目录）

调参期的扫描脚本一次跑好几个分支，**每个分支的结果落在各自的目录里，而汇总表只有一份**。
所以下面这四个 `.log` 没有同名目录——它们的用处是「那次批跑一共跑了什么、每段在哪个目录」：

| 批次日志 | 覆盖的目录 | 对应 README 的哪一段 |
| :--- | :--- | :--- |
| `step3_branchA.log` | `step3_critic_lr`、`step3_bias_lr`、`step3_bias_fixed`、`step3_polyak` | 「试过什么」表的 Critic 学习率 / 输出偏置 / Polyak 那几行 |
| `step3_retention.log` | `step3_clip` | 「试过什么」表的成功信号裁剪那一行（含**网格选错**的那条，边界第 9 条） |
| `step4_branchA2.log` | `step4_threshold`、`step4_gain`、`step4_postfactor`、`step4_critic_lr_low` | 「Critic 学不准，原因已经定位到读出」一节 |
| `step5_structural.log` | `step5_boltz_nocenter`、`step5_center5`、`step5_center20`、`step5_center80` | 「采纳规则」那两行 |

⚠️ **`step3_critic.log` 是 0 字节的空文件**，另外 `step3_critic/` 是个没有内容的空目录
（git 本来就不收录空目录）。那是一次**没有产出任何结果**的运行留下的壳子。按「不删除」的保留
策略原样留着——「哪一次跑了但什么也没出」也是记录的一部分，但**它不含任何数字，不要引用它**。

## 写论文时怎么用

- **要画曲线** → 用 json 里的 `curve`（每 50 回合一个评测点），不要用 `.log` 里的汇总行。
- **要报中位数/最小值** → 用 `.log` 的汇总表，或自己从目录里的 json 聚合（口径见上面「怎么读」）。
- **要报"验收"数字** → 只用 `step14_confirm`（全新种子 45–64）。`step13_lr001` 那批（25–44）
  是**用于调参**的种子，两批都过线，但两批之间约 27% 的差距本身就是结论的一部分（选择效应），
  这一点写在 W3 README 的「当前结论」里。
- **引用任何数字时**，连同**它出自哪个目录**一起写——这就是这份清单存在的理由。
