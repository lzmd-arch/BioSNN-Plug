# コントリビューションガイド

[中文](CONTRIBUTING.md) · [English](CONTRIBUTING.en.md)

時間を割いていただきありがとうございます。手間が無駄にならないよう、先に知っておくべき三つのことを述べます。

1. **これは研究プロトタイプであり、プロダクションフレームワークではありません。** 「明らかに入れるべき」に見える機能でも、
   プロジェクト計画書のロードマップに無いという理由で保留されます。
2. **メンテナは一人で、issue と PR への対応に SLA はありません。** 再現手順が揃った報告は優先的に扱われます。
3. **モダリティプラグインを書くのに、ここへ PR を出す必要はありません。** 独立したパッケージにすれば済みます。
   後述の「サードパーティプラグイン」の節を参照してください。

## 開発環境

[uv](https://docs.astral.sh/uv/)（Python のパッケージ・プロジェクト管理ツール）と Python 3.10+ が必要です。

```bash
git clone https://github.com/lzmd-arch/BioSNN-Plug.git
cd BioSNN-Plug
uv sync                    # 開発依存をまとめて導入（スケルトンライブラリは editable で入ります）
uv run pre-commit install  # 装提交钩子（首次需要联网拉取钩子仓库）
```

環境を確認します：

```bash
uv run pytest -q                                  # 测试全绿
uv run python examples/quickstart_register_plugin.py   # 示例能跑
```

## 提出前チェックリスト

プロジェクト計画書 §12.5 は、PR の前に「**引用スポット検証 + コードブロックのチェック**」を行うことを求めています。項目ごとに確認してください：

```bash
uv run pre-commit run --all-files    # 以下に挙げたチェックをまとめて実行します
```

| チェック | スクリプト | 何を止めるか |
| :--- | :--- | :--- |
| ユニットテスト | `pytest` | 実装の誤り |
| スタイル | `ruff check` / `ruff format` | 書式とよくあるバグ |
| ドキュメントのコードブロック | `scripts/check_doc_code_blocks.py` | ドキュメント内のコードが動かない |
| バージョン番号の一致 | `scripts/check_version_consistency.py` | ファイル名と内容のバージョン番号のずれ |
| 引用リスト | `scripts/check_references.py` | 番号の飛び、デッドリンクの書式、重複項目 |
| notebook の同期 | `scripts/build_notebook.py` | Colab notebook とサンプルスクリプトのずれ |

CI でも同じものを走らせ、さらに **lychee によるリンク生存確認**と**依存ライセンス監査**を加えます。

### ドキュメントのコードブロック：既定で実行、例外は明示的に注記

`scripts/check_doc_code_blocks.py` は Markdown 内の Python コードブロックを**実際に実行します**。
理由は、純粋な構文チェック（`ast.parse`）ではプロジェクト計画書 §12.3 が名指しした種類の問題を捉えられないからです——
`self.threshold` は構文としては完全に合法で、誤りは実行時にその属性が存在しないことにあります。

- 同じドキュメント内のコードブロックは**名前空間を共有し、出現順に実行されます**。チュートリアルを書くときは、スクリプトを書くときと同じように
  そのまま書き進められます；
- 単独では実行できない断片（サードパーティパッケージに依存する、単なる例示である）には、フェンスに注記します：

  ````text
  ```python no-run
  bus.register(get_plugin("audio")())   # このリポジトリに無いサードパーティパッケージに依存している
  ```
  ````

**`no-run` は「定義上動かない」コードのために用意された逃げ道であり、チェックを迂回する裏口ではありません。**
このリポジトリで普通に動くコードに `no-run` が付いていたら、レビューで戻すよう求められます。

### 引用スポット検証

外部引用を新規追加または変更するときは：

[`docs/references.md`](docs/references.ja.md) に登録し、**検証ステータス**を明記します。

検証のルール（何を四点確認するのか、各ステータスが何を意味するのか、確認できなかったときはどうするのか）はそのファイルが定義しています——そこが引用
ルールの唯一の帰属先です。このリポジトリでは実際に一度、クレジット表記の誤りが起きており、その原因もそこに記録されています。

## 変更の種類と互換性

| 変更 | 要件 |
| :--- | :--- |
| スケルトンライブラリ `packages/biosnn-bus/` | semver に従います。破壊的変更にはバージョン番号の更新が必要です；新たな能力は加法を優先し、既存のシグネチャは変えないでください |
| 研究コード `research/` | 最初の 12 か月（2027-09 まで）は互換性を約束しません。自由にリファクタリングできます |
| ドキュメント / ツールチェーン | 特別な要件はありません |

スケルトンライブラリは現在 `0.x` です。semver の取り決めでは、`0.x` のマイナーバージョンには破壊的変更を入れられます——これは意図的なもので、
外部の利用者がまだいないうちに素早く反復するためです。外部から依存される最初のバージョンで `1.0.0` に上げます。

### 重大なトレードオフを変えた場合

この PR が、後から見た人が「なぜそう書かなかったのか」と問うような決定を含むなら、**ADR を新たに 1 本追加します**。
書式と判断基準は [`docs/adr/README.md`](docs/adr/README.ja.md) を参照してください。

ADR は一度記録したら本文を変更しません——考えが変わったときは新しく 1 本書き、互いにリンクします。

## PR の流れ

1. Fork するかブランチを切ります（ブランチ名は `feat/...`、`fix/...`、`docs/...` を推奨）；
2. 変更 + テスト。**バグ修正には、そのバグを再現できるテストを添えてください**；
3. 提出前チェックリストを通します；
4. PR を出し、PR テンプレートのチェックリストを埋めます。

CI はすべてグリーンでなければなりません。現在の CI は Python 3.10 / 3.11 / 3.12 を対象とし、すべて CPU 上で実行します——
スケルトンライブラリは意図的に GPU に依存していません（[ADR-0002](docs/adr/ADR-0002-numpy-core-torch-optional.ja.md) 参照）。
ですから、自分の変更をテストするのに GPU が要ることに気づいたら、それはたいてい抽象の層を間違えたという意味です。

## サードパーティプラグイン

**本リポジトリに PR を出さないでください。** 自分の `pyproject.toml` で entry point を宣言するだけで済みます
（group は `biosnn_bus.plugins`）。本リポジトリ側の変更はゼロです。

設定の書き方と、利用者側からの組み込み方は
[`docs/plugin_guide.md`](docs/plugin_guide.ja.md#サードパーティパッケージからプラグインを提供する) を参照してください。

モダリティプラグインを作ったら、issue を立てて知らせてください。

## リリース

`biosnn-bus` は [`.github/workflows/release.yml`](.github/workflows/release.yml)
でリリースします：`v<version>` タグを push すると起動します。手順と、初回リリース前に必ず行う PyPI 側の設定
（trusted publishing の登録）は、いずれもそのファイル先頭のコメントに書かれています。

メンテナ向けの要約：

```bash
# 1. packages/biosnn-bus/pyproject.toml の version を変更し、__init__.py の __version__ も同期させる
#    （scripts/check_version_consistency.py がこの二か所 + 計画書のファイル名を照合します）
# 2. コミットして push した後にタグを打つ
git tag v0.1.0 && git push origin v0.1.0
```

`verify` job が先に tag 名とパッケージのバージョンの一致を確認します——tag を書き間違えればこの段階で止められ、
「v0.2.0 を出したのにパッケージの中身はまだ 0.1.0」といった後から直しにくい事態は起きません。

PyPI の設定が整うまでは、`gh workflow run release.yml` でパイプラインを手動で起動できます：
実行されるのは検証とビルドだけで、公開はしません。

## ドキュメントの翻訳

ドキュメントは中国語・英語・日本語の 3 版があります。管理対象のドキュメントを変更する
ときは、**3 版すべてを同時に変更してください**——古くなった翻訳は、翻訳がない状態より
も有害です。読者はそれを最新だと思って読むからです。

`scripts/check_translations.py` が 4 種類のずれを検出します。ローカルで実行して確認できます：

```bash
uv run python scripts/check_translations.py
```

| 検出するもの | どのように起こるか |
| :--- | :--- |
| 翻訳の欠落 | 新しいドキュメントに `.en.md` / `.ja.md` を付け忘れた |
| 見出し構造のずれ | 節が落ちた、あるいは `###` が `##` になった |
| コードブロックの破損 | 翻訳中にパラメータ値・識別子・コマンドを書き換えた |
| 別言語を指す内部リンク | ファイル名を変えたのにリンクを直していない |

**翻訳するのは本文であり、コードではありません。** コードブロック内のコメントと
docstring は翻訳してかまいませんが、変数名・コマンド・パス・`get_plugin("audio")` の
`"audio"` のような文字列リテラルは翻訳できません。そこを訳すとサンプルが動かなくなります。
CI はドキュメント内のコードブロックを実際に実行するため、この種の誤りは即座に露見します。

用語は [`docs/GLOSSARY.md`](docs/GLOSSARY.md) に従ってください。表にない新しい用語は、
先に表へ追加してから使います。

issue や pull request はどの言語で出していただいてもかまいません。

## 問題の報告

[issue テンプレート](https://github.com/lzmd-arch/BioSNN-Plug/issues/new/choose) を使ってください。
バグ報告には**最小再現スクリプト**と**環境情報**を必ず添えてください——テンプレートにはそのまま使えるコマンドが用意されています。

## ライセンス

コントリビュートすると、あなたのコントリビューションが [Apache-2.0](LICENSE)（コード）
または [CC-BY 4.0](https://creativecommons.org/licenses/by/4.0/)（ドキュメント）として公開されることに同意したものとみなされます。
